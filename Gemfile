#Create a separate file to install all programs with a single command
#File needs to be names EXACTLY "Gemfile"
#Command to run this is "bundle install"

source "https://rubygems.org"

gem "activesupport"
gem "awesome_print"
gem "pry-byebug"
